export interface Employee {
    id: number;
    name: string;
    email: string;
    role: string;
}

export interface EmployeeDto {
    id: number;
    name: string;
    email: string;
    role: string;
}